import React from 'react';

const Inventory = () => {
   const handleAddInvenroty = () => {

   }
   return (
      <div>
         <h3>This is inventory</h3>
         <button onClick={handleAddInvenroty}>Add Inventory</button>
      </div>
   );
};

export default Inventory;